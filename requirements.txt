confluent-kafka==2.3.0
pydantic==2.6.4
pydantic-settings==2.2.1
pyspark==3.5.1
redis==5.0.3
psycopg2-binary==2.9.9
SQLAlchemy==2.0.29
alembic==1.13.1
prometheus-client==0.20.0
faker==24.4.0
python-dotenv==1.0.1
structlog==24.1.0
tenacity==8.2.3
click==8.1.7

# --- Phase 2: ML, inference, explainability ---
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1.post1
xgboost==2.0.3
lightgbm==4.3.0
imbalanced-learn==0.12.0
shap==0.45.0
mlflow==2.11.3
fastapi==0.110.0
python-multipart==0.0.9
uvicorn[standard]==0.29.0
httpx==0.27.0
streamlit==1.32.2
plotly==5.20.0
requests==2.31.0
locust==2.25.0

# --- Phase 3: MLOps, observability, cloud ---
# NOTE: apache-airflow is deliberately NOT installed here. Airflow pins very
# strict transitive dependency versions ("constraints files") that regularly
# conflict with fastapi/mlflow/xgboost's own pins; bundling it into the same
# environment as the API/training images is a common source of dependency
# hell in real deployments. The Airflow webserver/scheduler run from the
# official apache/airflow image instead, with only the extra packages the
# DAGs need layered on top -- see docker/airflow/requirements.txt.
scipy==1.12.0
PyJWT==2.8.0
boto3==1.34.69

